#ifndef FILTER_H
#define FILTER_H

#include <iostream>

#include "../graph/Graph.h"
#include "Helpers/Other.h"

void filterAndWrite(Graph& graph, int girth, const runSetup& setup);


#endif //FILTER_H
